// Service Worker vazio para permitir a instalação do PWA.
// Pode adicionar lógica de cache mais complexa aqui mais tarde.
self.addEventListener('fetch', function(event) {
  // Deixamos vazio por agora para o setup mínimo.
});
